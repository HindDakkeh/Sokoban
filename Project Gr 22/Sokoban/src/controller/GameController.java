package controller;

public interface GameController {
	
	 /**
     * Adds a controller that refer to the Controller class
     * @param controller the controller object
     */
    void addController(Controller controller);

}
